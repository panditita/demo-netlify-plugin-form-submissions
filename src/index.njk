<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Demo - Form submissions Netlify plugin</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://api.mapbox.com/mapbox-gl-js/v2.5.1/mapbox-gl.css" rel="stylesheet">
    <script src="https://api.mapbox.com/mapbox-gl-js/v2.5.1/mapbox-gl.js" mode='cors'></script>

    <style>
      section {
        height: 50vh;
      }
      #map {
        position: absolute;
        top: 0;
        bottom: 0;
        width: 50%;
        margin: 15rem 0;
      }
    </style>
  </head>
  <body>
    <script src="https://api.mapbox.com/mapbox-gl-js/plugins/mapbox-gl-geocoder/v4.7.2/mapbox-gl-geocoder.min.js" mode='cors'></script>
    <link rel="stylesheet" href="https://api.mapbox.com/mapbox-gl-js/plugins/mapbox-gl-geocoder/v4.7.2/mapbox-gl-geocoder.css" type="text/css">
    <div class="tint">
      <header>
        <section>
          <h1>Form submissions plugin</h1>
          <p class="subtitle">A little demo of a Netlify plugin</p>
        </section>
      </header>
    </div>

    <main>
      <section>
        <ul class="submissions">
          {% for item in collection-form | reverse %}
            <li>
              <p>{{ item.data.lat}}</p>
              <p>{{ item.data.lng}}</p>
            </li>
          {% endfor %}
        </ul>
        <div id="map"></div>
        <a href='/form'>Submit a new archive</a>
      </section>

    </main>
    <script src="bundle.js" type="text/javascript"></script>

  </body>

</html>